--
-- Create a DB for persisting data related to secure hashing (e.g., salted SHA-2 hashes), encrypted secret values
-- for seeding a Deterministic Random Bit Generator (DRBG), and encrypted data encryption keys.
--
CREATE DATABASE security_store ;
